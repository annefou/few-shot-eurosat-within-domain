name: Run Few-Shot Experiment

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  experiment:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          pip install numpy matplotlib scipy jupytext

      - name: Run experiment
        env:
          CI: "true"
        run: python 01_few_shot_eurosat.py

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: results
          path: |
            results/few_shot_eurosat_results.json
            results/few_shot_eurosat.png

      - name: Print results
        run: cat results/few_shot_eurosat_results.json
